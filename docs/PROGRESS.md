# Implementation status

This is an implementation of `docs/plans/female-open-human-atlas-plan.md` up to composition 0.5
(2026-09-12, commit `553ecfb`). The full objective remains active. Do not interpret working rendering,
passing validators or licence checks as validation of a complete anatomical atlas: no
anatomist has reviewed any mesh, landmark or registration.

## Authoritative state

- Local fork: `ashemag/human-atlas`, base `7a383d3ee2759e3ddf157c704fb8814fd0c50bcb`, branch `female-open-atlas`.
- HRA Female v1.5: 888 meshes restored from `d72b4f6db42e41a8db84b1c19ff6d86ee7b65284`. Reference assembly, individual donors unresolved.
- BodyParts3D 4.0: 2,234 male reference meshes, selectable separately. Not used in the female composition. Its lateralized FMA identifiers now resolve through the crosswalk, so 1,221 of its meshes merge with female entries for comparison.
- TCIA Healthy Total Body CTs: subject 003, female, 26 years. 36 labels converted (1,049,570 optimized triangles). Registered to VHF by a pelvic-landmark similarity (RMS 12.07 mm) as an alternative source; no longer part of the composite (different donor, flexed knees).
- Denver VHF 2022 (`digitalcommons.du.edu/visiblehuman/1`): 128 final STL meshes (28 bones, 76 muscles, 16 cartilages, 8 ligaments), CC BY 4.0, native aligned VHF image frame, identity in the canonical space.
- NLM Visible Human Female fresh CT (new): terms verified (no licence since July 2019; attribution "Courtesy of the U.S. National Library of Medicine"), 1,734 GE slices and headers downloaded with SHA-256 (`data/raw/nlm-vhf/`), assembled into one 512 x 512 x 1734 RAS volume at 0.9375 x 0.9375 x 1 mm (`scripts/build-nlm-vhf-volume.py`; two exams, junction shift measured at 5.6 x 3.8 mm and recorded). Segmented with TotalSegmentator 2.18.0 `total` (Apache-2.0, CPU, 1.5 mm): 114 of 117 labels present. Stray islands below 5 % of each label's main component are removed and counted.
- MOOSE 3.2.2 bones on the NLM CT (2026-09-09, Apache-2.0 code, CC BY 4.0 weights, run on the RTX 3080 of rub-pc, 4.4 min): `data/derived/nlm-vhf/moose/` holds `clin_ct_peripheral_bones` (31 labels: humeri, radii, ulnae, carpals, metacarpals, fingers, scapulae, clavicles, skull, patellae, tibiae, fibulae, tarsals, metatarsals, toes; hand and foot bones grouped per side), `clin_ct_all_bones_v1`, `clin_ct_ribs`, `clin_ct_vertebrae`. Not yet ingested or meshed. Against TotalSegmentator `total` on the same CT (`generated/moose-vs-totalseg.json`): femora Dice 0.96, humeri and hips 0.92, ribs 0.68 to 0.75, L1 to L3 0.39 to 0.65 with 17 to 21 mm centroid offsets (lumbar naming unresolved between the two models). The right ulna and radius touch the CT field-of-view edge: the fresh CT cuts the right forearm, which only the cryosections can supply. Plan: `docs/plans/vhf-cryosection-machine-driven-plan.md` (plan B, two review rounds folded in), reviewer reply in `docs/plans/vhf-cryosection-review-reply.md`. CT field-of-view coverage (`scripts/ct-coverage-mask.py`, `generated/nlm-ct-coverage.json`, mask in `data/derived/nlm-vhf/ct-coverage-mask.nii.gz`): body on the edge in 423 slices (arms z 290-679, head z 123-151); outside coverage the CT is unknown, not air. Denver-versus-CT baseline (`scripts/denver-ct-baseline.py`, `generated/denver-ct-baseline.json`): per-bone rigid fit to the HU = 300 edge, shape p95 median 1.96 mm (max 3.46, 22 bones; 6 small tarsals diverge), placement p95 median 7.3 mm under `nlm-ct-to-vhf`. NLM colour cryosections downloaded to rub-pc (`/media/rub/Backups/VHF/Female-Images/fullbody`, 5,186 slices, 22.8 GB, SHA-256 manifest); Denver aligned cryosection DICOM downloaded (`data/raw/denver/aligned-cryosection-dicom.zip`, 485 MB); aligned CT and original label maps in progress (`scripts/fetch-denver.py` now streams downloads through the browser).
- CT prior consensus and laterality (2026-09-09, `scripts/ct-prior-consensus.py`, `generated/ct-prior-consensus-{nlm,denver}.json`). Third model added: Skellytour `high` (60 labels: skull, pelvis, sternum, femora, humeri, scapulae, clavicles, 24 ribs, 24 vertebrae; no tibia, fibula, forearm, hands or feet), run on the RTX 3080 of rub-pc in z-chunks (its own estimate is 38 GB RAM per 109 L of CT at `high`; the box has 31 GB), stitched to `data/derived/nlm-vhf/skellytour/skellytour_high.nii.gz`. NLM CT, three models: 14 bones agree (all pairwise Dice >= 0.80), 46 partial, 1 disagree, 11 MOOSE-only. TotalSegmentator and Skellytour agree on the cervical and thoracic vertebrae (Dice 0.90 to 0.95); MOOSE is the outlier (cervical 0.55 to 0.65 against both). In the lumbar spine every model is wrong somewhere (see the lumbar finding below): the nearest-centroid name matching that first suggested "no numbering shift" is not one-to-one and was superseded by the instance analysis (`scripts/ct-vertebra-instances.py`). Femora 0.97, humeri 0.89 to 0.93, clavicles 0.83 to 0.94, sternum 0.87 to 0.93; ribs and scapulae 0.62 to 0.85 with Skellytour closer to TotalSegmentator than MOOSE. Denver aligned CT, TotalSegmentator + MOOSE: 7 agree, 42 partial, 12 disagree (C1 to C7 0.35 to 0.46, six ribs); agreement is lower than on the NLM CT for every bone class, so the NLM fresh CT is the preferred prior source and the Denver CT a cross-check. Denver CT with Skellytour added (`data/derived/denver/priors/skellytour/skellytour_high.nii.gz`, 12 chunks of 150 slices): 7 agree, 38 partial, 16 disagree, 11 MOOSE-only; Skellytour again matches TotalSegmentator on the cervical and thoracic spine (0.79 to 0.94) and MOOSE is the outlier. Lumbar finding, both CTs, slice-by-slice profile of the three models: the donor has six lumbar-type vertebral bodies between the last rib-bearing vertebra and the sacrum (NLM CT z 556 to 740, 184 mm; Denver CT z 980 to 1190). Each model forces five names onto six bodies in a different way (Skellytour labels two bodies `L2` on the NLM CT and leaves one body unlabelled on the Denver CT; TotalSegmentator alternates L2/L1/L2; MOOSE gives L3 two bodies), which is the whole L1 to L3 disagreement (L2 Dice 0.39, 0.19 and 0.01 in the worst pairs). Vertebra consensus must therefore work on instances (connected bodies) and not on names, and the numbering (lumbarised S1, extra segment or rib count) is an anatomist question recorded in `registry/review-status.json`. Laterality passes three consistency checks on both CTs (they share TotalSegmentator organ labels and the CT header, so they are not independent): organ anchor (liver and gallbladder at RAS x > 0, spleen and stomach at x < 0), every left/right bone label of every model on its side (152 labels on the NLM CT, 120 on the Denver CT, 0 mismatches), and, on the Denver CT, the liver and every paired bone carried by `denver-aligned-ct-voxel-to-vhf` land on the side of the Denver `Right`/`Left` meshes. This resolves the mirror ambiguity of the pelvis-only placement (flips [-1,1,1] p95 5.4 mm against [1,1,1] p95 6.5 mm) as far as the organ labels are right: the chosen handedness is the one consistent with liver right and spleen left, and the Denver label-map frame is +x = subject right as `transforms/canonical-space.json` states (the `to` text of the transform file says LPS; the numbers say otherwise).
- Vertebra consensus by instance (2026-09-09, `scripts/ct-vertebra-instances.py nlm|denver --selftest`, `generated/ct-vertebra-instances-{nlm,denver}.json`, panels in `generated/ct-vertebra-instances-{nlm,denver}/`, label maps `vertebra-{instances,review,votes,eligible}.nii.gz` under `data/derived/nlm-vhf/consensus/` and `data/derived/denver/priors/consensus/`). Names and counts are observed, not imposed: connected components of every vertebral label of every model (including TotalSegmentator `S1`, MOOSE `L6`, both `sacrum`) are candidates; correspondence between models is by voxel overlap and craniocaudal order with explicit states (matched, split, merge, partial, unmatched); candidates are grouped into instances with neutral ids V01.. (cranial to caudal), groups where one model splits what another merges are partitioned by the finest model unless its extra boundary is unsupported (no other model separates the two parts and a normal-sized candidate of another model covers both); one vote per model per instance, strict majority of the *eligible* models per voxel, with `unsupported` (no class), `unprocessed` (outside Skellytour's body crop) and `absorbed` (the voxels carry another label of that model, e.g. sacrum or PELVIS) kept apart from `negative`. Result on both CTs: **25 consensus vertebrae** (7 cervical, 12 thoracic, 6 lumbar-type bodies below the last rib-bearing vertebra) plus the sacrum, and one further TotalSegmentator-only body (`S1`, about 49 mL) that MOOSE labels sacrum and Skellytour pelvis. Same-donor external evidence: the HRA united-female v1.5 skeleton (modelled on the Visible Human Female) has 7 + 12 + 6 = 25 vertebrae and a fused sacrum, and NIH 3D entry 3DPX-020988 states "The Visible Human Female has 6 lumbar vertebrae"; matched by order from the sacrum, the HRA chain gives every instance a candidate name (V20 = L1 ... L6 for the sixth lumbar-type body) with z offsets within 7 mm on the NLM CT (sacrum 4.2 mm) and within 10 mm in the lumbar spine on the Denver CT. These names are recorded as `hra_name_by_order` with provenance; `name_status` stays `pending` for every instance (`registry/review-status.json`). Open: whether the `S1` body is the first sacral segment of a normal sacrum below six lumbar vertebrae or a further transitional segment. Self-test (merge two bodies, split one, delete one, swap two names, shift one 15 mm) passes on both CTs. Detached pieces (union volume below half the family median) are merged into the one instance within 10 mm by surface distance (anisotropic spacing honoured), or into the nearest when it is at least twice as close as the next; otherwise they stay as `piece` or `ambiguous` proposals. Names never decide; label agreement with the target is recorded as evidence. Per-model instance maps (`*-model-<model>.nii.gz`) keep every alternative, and `union_ml`, `conflict_voxels` and `lost_to_other_winner_ml` are computed on the true per-model union, not on the winner map. Skellytour eligibility comes from the merge manifest (completed chunk cores); an incomplete manifest is refused. `scripts/test-ct-instances.py` runs 23 end-to-end scenarios on synthetic volumes (renamed labels through the whole pipeline, merge, split, missing body, missing chunk, near and far pieces, contested boundaries, anisotropic nearest-seed split). The same script runs on the rib families (`nlm|denver ribs_left|ribs_right`, `generated/ct-rib-instances-<ct>-<side>.json`, `rib-<side>-*.nii.gz`): **12 consensus ribs per side on both CTs**, every rib matched by all three models (unanimous fraction 0.42 to 0.69, boundary disagreement at 1 mm), plus one MOOSE-only `rib_8` fragment per CT on the left kept for review; the models' rib names agree everywhere, so no discrepancy was detected in rib count or names; `name_status` stays `pending` like every other instance. Skellytour chunk seams checked from the stitched volumes (`scripts/skellytour-seam-check.py`, `generated/skellytour-seams-{nlm,denver}.json`): no vertebra loses its identity across a seam; the merge script now refuses missing or ambiguous chunks and records provenance (`scripts/gpu/merge-skellytour.py`; re-run on rub-pc the same night: byte-identical volumes, manifests with SHA-256 per chunk copied to `data/derived/.../skellytour/skellytour_high.json`).
- New source `ct-consensus` (2026-09-10, `scripts/ingest-ct-consensus.py`, `public/models/atlas-ct-consensus.json`, `generated/ct-consensus-qa.json`): 50 meshes from the NLM-CT instance maps (25 vertebrae, 1 sacrum, 24 ribs; 412 k optimized triangles, 4.3 MB gzip). Mesh ids are the geometric instance ids (`CTCONS:VHF:V20`, `RL06`); the candidate anatomical name (HRA same-donor chain by order for vertebrae, the shared rib number for ribs) is metadata with `name_status: pending`, together with the per-instance vote figures (consensus and union volume, agreement ratio, unanimous fraction, conflict voxels, per-model state and label). Three review-only instances (two MOOSE fragments, the TotalSegmentator-only `S1` body) are listed in the atlas and not meshed. No ontology term is assigned: the catalog carries the geometric ids until a name is documented. Composition 0.5 replaces the single-model CT vertebra and rib labels by the consensus instances (the consensus sacrum yields to the Denver sacrum); the composite keeps 1,015 meshes (786 HRA, 128 Denver, 52 CT labels, 49 consensus). Validators updated (`validate-composition.py` requires 25 consensus vertebrae and 24 ribs and no single-model vertebra or rib label; provenance, licence, review, geometry QA and anatomy QA enumerate the new source). Viewer: source `Female VHF CT consensus · instances`.
- Canonical space `VHF-image-2022` (`transforms/canonical-space.json`): the Denver aligned VHF image frame, now verified against the NLM CT header frame. Rigid same-donor registration of the CT hip bones and sacrum onto the Denver pelvis (`transforms/nlm-ct-to-vhf.json`): rotation 2.17 deg, free-scale check 1.0013, pelvis surface p95 4.72 mm (hip bones 2.4 mm, sacrum 9.8 mm, femora 7.5 to 8.7 mm under the pelvis transform; own femur fits 3.4 to 4.4 mm with hip pose changes of 1.6 and 3.0 deg between acquisitions). `generated/nlm-ct-registration.json`.
- New source `nlm-vhf-ct`: 114 meshes, 1,431,628 optimized triangles, 14 MB gzip, natively in the canonical stage (voxel -> CT RAS -> `nlm-ct-to-vhf` -> stage). Automatic labels, unreviewed.
- Reviewed ontology crosswalk (`registry/crosswalk-proposals.json` -> `scripts/build-crosswalk.py` -> `registry/ontology-crosswalk-reviewed.json`): every entry carries the OLS record (IRI, label, synonyms, xrefs, definition) and match type. Applied: Denver 69/69, TCIA 35/36, NLM CT 114/117, HRA lateralized FMA 176/201, BodyParts3D lateralized FMA 1,221/1,360; 168 unresolved or uncertain entries keep their source identifiers. 1,712 meshes carry a crosswalk resolution; 248 catalog entries now have several sources; 22 entries merge Denver with HRA (femur, tibia, fibula, patella, sacrum, coccyx, knee cartilage and ligaments, lower-limb muscles). Anatomist review pending for every equivalence.
- Experimental composition 0.5 (2026-09-10): 1,015 meshes. Denver (128, identity) + NLM VHF CT (52 labels; Denver replaces the CT hip bones, sacrum, femora and gluteal/iliopsoas labels; the consensus instances replace the CT vertebrae and ribs) + CT consensus (49 instances) + HRA detail (786 meshes; skeleton, skin, lower-limb muscles and organs already covered by a same-donor CT label excluded). HRA fitted by a similarity on six organ bounding-box proxies onto the CT organs: RMS 7.42 mm (was 29.1 mm through TCIA); head by a bounding-box fit into the CT brain envelope. 229 composite meshes come from the single VHF donor; 786 from the HRA assembly.
- Bone landmarks (`scripts/extract-landmarks.py`, `transforms/landmarks/*.json`): 18 per source (Denver, TCIA 003, HRA), automatic geometric rules, `manual_review: pending`. The viewer's Registration review panel draws the pairs and exports reviewer decisions to `landmark-review.json` (intake: `registry/landmark-review.json`).
- BMFToolkit: LICENSE is zlib with no data carve-out (Zenodo `other-open`); request to the authors drafted in `docs/LICENSING.md`, not sent. Compared bone by bone with Denver (`generated/bmftoolkit-comparison.json`): segmented right bones median p95 1.7 mm, mirrored left 2.6 mm, symmetrized sacrum 15.6 mm. Not shipped.
- Anatomy QA (`scripts/qa-anatomy.py`, `generated/anatomy-qa.json`, recomputed for composition 0.5 on 2026-09-12): self-intersecting triangle pairs per mesh (edge-triangle tests), connected components and outlier components, composite bounding-box continuity. Meshes with self-intersections: Denver 1 of 128, TCIA 36 of 36 (voxel meshes), NLM CT 101 of 114, CT consensus 50 of 50 (median 6 pairs, max 23; same voxel-mesh pipeline as the NLM CT labels, not repaired), HRA 89 of 888, composite 180 of 1,015, BodyParts3D 2,060 of 2,234. Outlier components: TCIA 16 labels (`Toes` above the head), NLM CT 1, HRA 4, composite 5, consensus 0. Continuity (bounding boxes only): the spine selector finds the 25 consensus vertebrae (`role: vertebra`); HRA head structures overlap the top of the consensus vertebral column by 5.7 mm and the HRA brain lies inside the CT skull box; the consensus column (no `S1` body) ends 14.5 mm below the Denver sacrum top (was 39 mm with the single-model column that included `S1`). Review decisions live in `registry/review-status.json` (default `pending`, three known issues recorded).
- QA identity and carry-over (2026-09-12, after two external code audits of the 0.5 ingestion, commits `083589a` and `553ecfb`): the ingestion had shipped every QA field as `not-assessed` because `qa-geometry.py` ran without `qa-anatomy.py`, and the composite copied source provenance before QA ran, so the 49 consensus instances had no QA metadata in the viewer. Now `scripts/qa_identity.py` defines the only rule: a measured result follows a mesh only when its `geometry_sha256` (SHA-256 of the shipped float32 vertices and uint32 faces) equals the digest in the previous report; aggregate statistics never establish identity (a translated closed component keeps triangle count, edge counts and signed volume) and rows without a digest are never reused. `scripts/qa-hash-revision.py REV REPORT OUT` migrates an older report from the buffers of the git revision that produced it; the 4,366 rows carried over from the 0.4 report were verified byte-identical to f830ea5. `scripts/test-qa-carryover.py` is the regression test. `qa-geometry.py` carries identical meshes over and marks the rest `not-assessed`; `qa-anatomy.py` reuses identical meshes and recomputes the rest (minutes instead of 45 min), keeps atlases not named, and refuses to write null spine measurements when CT parts exist. `build-registry.py`, run after both passes, refreshes `public/atlases/composed.json`: every composed part carries `geometry_qa` (source mesh) and `composed_geometry_qa` (transformed copy) and the build fails if a row is missing; `validate-composition.py` checks both digests against the shipped buffers and against `generated/qa-report.json`. Mandatory order: compose -> qa-geometry -> qa-anatomy -> build-registry -> validators (`docs/REPRODUCIBILITY.md`).
- Licence verification 2026-09-05 (`datasets.csv`): NLM terms; SPIDER, OpenEar, HiP-CT CC BY 4.0 (specimen selection pending); Teeth3DS CC BY-NC-ND (cannot be shipped); Z-Anatomy CC BY-SA (male-derived, share-alike target only); SPARC per dataset; BMFToolkit zlib scope unconfirmed.
- Model agreement exposed (2026-09-12): every `ct-consensus` mesh shows, in the source and in the composite, the per-instance vote recorded at ingestion: consensus and union volume, agreement ratio (consensus / union), unanimous fraction (over consensus voxels), eligible-model and vote histograms, conflict voxels, and each model's state with a gloss that keeps `unsupported`, `unprocessed` and `absorbed` apart from `negative`. Wording is "agreement between models"; the panel states that it is not a probability of correctness, not anatomical confidence and not independent validation; `name_status` stays pending. `scripts/validate-consensus-metadata.py` checks that the source atlas, the manifest, the composite and the instance tables agree field by field and that the figures are internally consistent; `scripts/test-agreement-panel.mjs` checks the wording and the rendering of null, zero and missing values (50 instances rendered; 142 matched, 4 merge, 3 seed, 1 unsupported states).
- Per-name bone candidates, gated (2026-09-12, `scripts/ct-bone-consensus.py nlm --selftest`, `registry/ct-label-equivalence.json`, `generated/ct-bone-consensus-nlm.json`, label maps `data/derived/nlm-vhf/consensus/bone-{consensus,single-model}.nii.gz`): for the 34 non-vertebra, non-rib classes the three vocabularies name alike, the class equivalence registry (group labels never meet individual bones), the largest-component correspondence (IoU >= 0.50 and centroid <= 15 mm per pair), the laterality anchor and the coverage/eligibility gates run before the strict-majority vote; the figures are those of the instance tables. Result, all `machine-unverified`, nothing composed: 9 `candidate-consensus` (femora 0.95/0.96 agreement/unanimous, hips 0.86/1.00 with Skellytour `absorbed` into PELVIS, clavicles 0.81-0.83, right scapula 0.72/0.76, skull 0.79/0.77, sternum 0.83/0.88); 3 `not-accepted` (left scapula agreement 0.699 under the 0.70 floor; both humeri, 7 % and 16 % of their surface on the CT field-of-view edge, so the acquisition cuts them); 22 `candidate-single-model` (MOOSE-only patellae, tibiae, fibulae, radii, ulnae and the hand and foot groups; agreement undefined, not perfect; forearms also on the FOV edge); sacrum excluded (instance consensus). No pair failed geometry or laterality. Comparison, not substitution: consensus femora against the Denver femora p95 7.3-8.4 mm surface-to-vertex under `nlm-ct-to-vhf` (posture and registration included), hips 3.1 mm; MOOSE patellae 7-9 mm, tibiae 11-12 mm, fibulae 13-14 mm against Denver; against the current `nlm-vhf-ct` label Dice 0.97-0.99 for femora and hips. Denver stays where it is used; no consensus mesh enters the composite without a recorded per-bone decision (plan B section 2.6).
- Bone candidates ingested (2026-09-12 night): the 9 `candidate-consensus` bones are meshed as family `bones` of source `ct-consensus` (`CTCONS:VHF:B01..B34`, 59 source meshes in total, 755,770 optimized triangles, 4 LOD chunks, 4.1 MB gzip). Each carries the vote figures, the four gates, `consensus_status`, `review_status: machine-unverified`, `name_status: pending`, and the comparison with the `nlm-vhf-ct` label (Dice 0.97-0.99 for femora and hips) and with the Denver mesh where one exists (femora p95 7.3-8.4 mm, hips 3.1 mm, registration and posture included). Their structure id is the crosswalk term of the shared label, so each candidate is an alternative of the same catalogue entry as the CT label and the Denver mesh: coverage entries for femora, hips, clavicles, right scapula, skull and sternum list `ct-consensus` among their candidates, `best_available` stays Denver where Denver exists and the `nlm-vhf-ct` label elsewhere (machine-unverified records never win a priority tie). Not composed: the composite keeps its 1,015 meshes; `compose-female.py`, `validate-composition.py` and `validate-consensus-metadata.py` enforce it. QA measured for the nine (all have self-intersections, 1 to 130 pairs; the skull candidate has 43 components). The viewer panel shows the gates and the comparison under the heading "Comparison, not substitution". Not-accepted and single-model candidates are listed in `review_only_bone_candidates` and not meshed. Stage of plan B section 2.6 at ingestion (2026-09-12): gates and vote only; the shape check ran on 2026-09-13 (next bullet but one). `candidate-consensus` remains the ingestion status; acceptance under section 2.6 is recorded per bone in `shape_check.decision`.
- Validator hardening (2026-09-12, Codex audit of f28467f, finding 1): `validate-consensus-metadata.py` had not compared `gates`, `versus_nlm_vhf_ct_label` or `versus_denver_mesh` with the bone report or between copies (a corrupted in-memory atlas passed with `copies_equal: true`; the shipped files were not corrupted). Now those fields plus `denver_mesh`, `bone_class`, `review_status` and `consensus_status` are compared report = source atlas = manifest, the gate records are checked internally, the CT-label comparison is mandatory and the Denver comparison mandatory where a Denver mesh exists. `scripts/test-consensus-metadata.py` reproduces the audit's in-memory corruptions and expects the validator to fail (11 cases, including a shape check bound to another geometry, a flipped decision and a dropped record).
- Shape check of plan B section 2.6 run (2026-09-13, `scripts/ct_edge_fit.py` shared procedure, `scripts/denver-ct-baseline.py` refactored onto it with the 28 Denver figures unchanged, `scripts/ct-candidate-shape-check.py nlm`, `generated/ct-candidate-shape-check-nlm.json`, result bound to the `geometry_sha256` of each shipped mesh and copied into the provenance as `shape_check`): own rigid fit of the shipped candidate to the HU = 300 edge of the fresh CT, criterion shape p95 <= the Denver bone of the same class and side. Femora reach the baseline (left 1.83 mm vs Denver 2.85, right 1.86 vs 2.54); hips do not (left 1.38 vs 0.93, right 1.43 vs 1.19); clavicles 1.31/1.50, right scapula 1.62, skull 1.81 and sternum 1.93 mm have no class baseline (Denver covers pelvis to feet) and stay `no-class-baseline`, figures for information only. Placement as registered p95 1.4 to 2.0 mm for all nine (the candidate comes from the same CT, so this is near zero by construction). Caveat: a same-CT candidate is compared with its own CT's edge, so the figure measures the label boundary against the threshold, not independent geometry; Denver's residual compares a cryosection-derived surface with the CT edge. Result: two candidates (femora) meet every criterion of section 2.6; nothing is composed and no substitution is decided; the viewer shows a "Shape check (plan B 2.6)" section per candidate and `validate-consensus-metadata.py` requires the record on every candidate. Geometry unchanged: 4,474 QA rows carried over by digest, bins untouched.
- Block C started: inventory of the NLM colour cryosections (2026-09-13, `scripts/inventory-cryosections.py`, run on rub-pc, `generated/cryosection-inventory.json`, thumbnail `generated/cryosection-inventory-avf1800a-thumb.png`). The 5,186 files under `/media/rub/Backups/VHF/Female-Images/fullbody` all match the SHA-256 manifest and all decompress to 2,048 x 1,216 x 3 bytes. Byte layout is planar (R plane, G plane, B plane), verified visually on `avf1800a` (interleaved reading gives a grey image). Naming `avfNNNN{a,b,c}.raw.Z`: NNNN = 1,001 to 2,730 (1,730 millimetre positions), a/b/c = the three 0.33 mm sub-slices, so a complete set would be 5,190 files. Local integrity and origin completeness are kept apart. Local integrity (the inventory): every file on disk matches the SHA-256 manifest that travelled with the download and decompresses to the expected size. Origin completeness (`scripts/check-cryosection-origin.py`, `generated/cryosection-origin-check.json`, 2026-09-13): NLM's own 1996 recursive listing shipped with the data set (`data/raw/nlm-vhf/INDEX`, section `./Fullcolor/fullbody`) has exactly the same 5,186 files with the same byte sizes, and the current NLM server answers HTTP 403 for the four absent names (`2328c`, `2329a`, `2329b`, `2730c`) while it answers 200 for their neighbours, so the four were absent from the origin listing in 1996 and are not served today; the 21 present placeholders (5,454 compressed bytes, all-black) are also placeholders in the origin listing. The NLM documentation figure of 5,189 slices matches neither 5,186 nor 5,165 and stays recorded as unexplained. Placeholder groups: `1492b` to `1497b` (16 slices, 5.33 mm of missing slices, 5.67 mm between the bounding valid slices `1492a` and `1497c`), `1977c` to `1978b` (3 slices, 1.0 mm) and `2329c` to `2330a` (2 slices, contiguous with the three absent ones `2328c`, `2329a`, `2329b`: 5 slices, 1.67 mm of missing slices). Usable slices: 5,165. No download is needed: the origin has nothing more to fetch for this set. The next stage 0 steps are the trunk posture offset and the alignment check against Denver.
- **Correction (2026-09-13, afternoon): Denver's aligned CT is the NLM fresh CT, resampled, not a CT of the frozen block.** Evidence: the NLM female data set has no frozen CT (`data/raw/nlm-vhf/INDEX` lists only `radiological/normalCT`); sampling both volumes at the same canonical points after each one's own placement gives NCC 0.945 at the pelvis, 0.899 in the thorax, 0.998 in the head and 0.993 at the knee after a local translation of 2 to 6 mm, with equal HU means; a scan of the frozen body would differ in gas, HU and posture. So the bullet below compared two placements of one acquisition (`nlm-ct-to-vhf` against `denver-aligned-ct-voxel-to-vhf`, both rigid pelvis fits) and its figures are placement inconsistency plus resampling, not posture: the 3.35 mm pelvis-anchor offset and the 2 to 5 mm growth towards the head are what two pelvis fits of the same CT disagree by at 600 mm. The fresh-versus-frozen posture was measured for the first time by the photograph-to-CT check (bullet further below): about 3.6 deg about the left-right axis over the trunk. The `posture_offset` and `trunk_posture_context` fields shipped in every trunk CT mesh keep their figures but their name and gloss are wrong; renaming them (ingests, validator wording, panel, tests) touches the pipeline and is listed in the backlog for a decision.
- Trunk posture offset measured (2026-09-13, plan B stage 0 first item, `scripts/trunk-posture-offset.py`, ~70 s, `generated/trunk-posture-offset.json` + `.png`): the same consensus instances (25 vertebrae, sacrum, 24 ribs) on the NLM fresh CT (`nlm-ct-to-vhf`) and on Denver's aligned CT (`denver-aligned-ct-voxel-to-vhf`), each CT placed by its own rigid pelvis fit, compared in `VHF-image-2022` (Denver minus NLM): centroid offset, surface distance as placed, per-instance rigid ICP (rotation and residual), the offset recomputed with each model's own instance map (segmentation part of the uncertainty), pelvis and skull anchors, Kabsch chain fits and a neighbour control (each NLM vertebra against the next caudal Denver instance: 20 to 25 mm, so the method sees displacement). Result: a pelvis-anchor offset shared by every level (hips + sacrum: x -0.15, y +0.86, z -3.23 mm, norm 3.35 mm) whose cause this calculation does not identify (registration translation, angular registration error or anchor segmentation; subtracting it removes one translational reference only, and 0.5 deg of angular error alone gives about 5 mm at 600 mm from the pelvis); relative to the pelvis the offset grows smoothly from 0.8 to 1.0 mm at the lowest lumbar-type bodies and the sacrum, to about 2 mm posterior through the thoracic spine (norm 1.7 to 2.7 mm), 2.5 to 4 mm at C4 to C7 and 4.8 to 5.0 mm at C1 to C3 (4 mm posterior, 2.7 mm inferior); the skull anchor moves 6.8 mm as placed with a 3.8 deg own fit. Per-instance own-fit rotations are below 0.5 deg except V05 (4.3 deg), V22 (1.8 deg) and V25 (1.3 deg), whose after-fit residuals (2.0 to 2.7 mm p95 against 0.7 to 1.0 mm elsewhere) are larger; the residual mixes segmentation differences with discretisation, sampling and fit limits and is not attributed. A heuristic discrepancy threshold is recorded per level, not an error bound and not a confidence level (it propagates no angular uncertainty, the models may share bias, and a per-axis range is not a Euclidean range): absolute = pelvis-anchor offset 3.35 mm + the largest per-component range across the three models (1.7 to 4.3 mm); relative = the largest disagreement between the three pelvis anchors about the anchor offset (0.76 mm) + the model range. Only C1 to C3 lie beyond both; being beyond marks a discrepancy to inspect and attributes nothing to posture. Ribs: 1.6 to 6.2 mm relative to the pelvis, largest at RL06, RL07, RL10 and RR06 (ribs are thin and the vote boundaries differ). Chain fits are flagged: the vertebra centroids are nearly collinear (second singular value below 15 % of the first on every region), so the rotation about the spine axis and the region translations are not physical and only the residuals are read (whole spine RMS 1.37 mm, thoracic 0.23 mm). What this does not separate: posture from registration (translation and angle) from segmentation, the pelvis-anchor offset included; nothing is corrected, no level is named, and nothing is anatomy. Codex audit of 38308be (2026-09-13, three P2, all applied the same day): a label that voted into two consensus instances (`vertebrae_L1` -> V20 and V21, the six-lumbar split) keeps every linked instance in `trunk_posture_context.linked_instance_ids` / `linked_levels`, figures from the linked instance nearest in z to the mesh centroid (validator checks the set, the choice and the list); the anchor offset is no longer called a registration difference; the sum is called a heuristic threshold. Published in the viewer: every `ct-consensus` vertebra, rib and sacrum mesh carries `posture_offset` (report hash-bound; centroid offset, relative-to-pelvis offset, own fit, heuristic discrepancy threshold, pelvis-anchor offset) and every `nlm-vhf-ct` mesh carries `trunk_posture_context` (the instance its TotalSegmentator label voted into, e.g. `vertebrae_L1` -> V21, or the nearest measured level by z; outside C1 to sacrum marked context only); `build-registry.py` copies both into manifests and composite unchanged, `validate-consensus-metadata.py` checks the hash binding, the figures against the report and the wording (never validation), `test-consensus-metadata.py` has six posture corruption cases (17 in total) and `test-agreement-panel.mjs` renders the panel for both record kinds. Consequence for stage 1: the fresh-CT trunk labels enter the cryosection frame as priors with this per-level measured discrepancy (about 2 mm relative to the pelvis through the thoracic and lumbar spine, about 5 mm at the upper cervical spine and skull, plus the 3.35 mm pelvis-anchor offset of unidentified cause); nothing is corrected from these figures, and any correction needs its own protocol (sensitivity to the registrations and models, fixed before use) after the alignment check against Denver's aligned slices.
- Alignment of the colour photographs against Denver's aligned slices (2026-09-13, plan B stage 0 second item, `scripts/check-cryosection-alignment.py` run on rub-pc: about 50 min for the 3,533 Denver slices, a seeded third pass of 60 slices in 14 s; compact report `generated/cryosection-alignment-check.json`, full report with rotation grids and per-block rows in `data/derived/nlm-vhf/cryosections/` and on rub-pc; transform `transforms/nlm-cryosection-to-vhf.json` written by `scripts/build-cryosection-transform.py`). Denver's aligned grayscale slices are the NLM photographs mirrored left-right, converted to gray with weights close to Rec. 709 (median fit 0.218 R + 0.710 G + 0.089 B, RMS 2.3 gray levels), averaged 2 x 2 (one Denver pixel = 2.0000 photograph pixels, largest deviation 0.0005 over 3,497 slices, so the photograph pixel is 0.333 mm through Denver's stated 0.666 mm) and registered as three rigid blocks: mid-thigh to pelvis (Denver k 2285 to 3532, z 761 to 1,176 mm, photographs 1970a to 1554b) on the photograph grid without rotation (Denver column 0 = photograph column 1,724.5, row 0 = row 86.5, mirrored); knee to mid-thigh (k 1210 to 2259, photographs 2328b to 1978c) unrotated and shifted 6.0 px in x and 4.0 px in y (2.0 and 1.3 mm) relative to the pelvis block; feet to knee (k 7 to 1207, photographs 2730b to 2330b) rotated 1.562 deg with its centre 14.5 px (4.8 mm) from the pelvis block. Within each block the per-slice parameters deviate from the block median by at most 0.09 px (p95) and 0.006 deg, and the residual after the per-slice similarity (3 x 3 blocks, phase correlation) is median 0.00, p95 0.033, max 0.09 photograph pixels (0.03 mm): the stage 0 criterion (mean residual below one pixel, image to image, pelvis to feet) is met on 3,497 of 3,497 non-blank slices. Slice identity: the handwritten slice number photographed on a card with every cut is visible in 2,640 Denver slices and identifies the photograph (label NCC and whole-frame NCC agree on all 2,640; neighbouring photographs 1/3 mm apart differ too little in tissue for the frame alone, which mis-identified 29 slices before the label and the seeded passes); above k 2285 the card leaves Denver's crop and the whole frame decides with margins of 0.003 to 0.006 and one constant offset. Denver k = 5192 - n (n = photograph index, 3 (NNNN - 1001) + {a, b, c}) in the two upper blocks and 5195 - n below the knee: at the NLM gap 2328c to 2330a (five missing photographs, 1.67 mm) Denver keeps two blank slices (k 1208, 1209), so everything below the knee lies 1.0 mm closer to the pelvis in Denver's z than a uniform 1/3 mm cutting of the photograph indices would place it; which of the two is physically right is unknown (the five photographs do not exist). Denver blank slices, 36: k 0 to 6 (beyond photograph 2730b), 1208 to 1209 (the gap), 1894 to 1895 (photographs 2100a and 2100b, which exist and are valid) and 2260 to 2284 (photographs 1970b to 1977b, 22 valid photographs, plus the 1977c to 1978b placeholders): 8.3 mm of mid-thigh without a Denver reference. Photograph axes measured: columns increase towards the subject's left, rows towards anterior (the patella lies at larger row than the femur in the Denver label map), the index towards the feet; the inventory note that rows run anterior to posterior was a documentation value and is corrected. Codex audit of 02d3f59 (2026-09-13, two P2, applied the same day): the transform's per-slice table now keeps the identity evidence (`identity_by`, `label_margin`, `frame_margin`, `identity_status`, `resolution`, `provisional`): 2,640 slices `settled` (card and frame agree), 637 `frame-with-margin` (no card in the crop, frame margin at least 0.005, block offset), 220 `provisional-block-consistent` (no card, frame margin below 0.005: the block offset is consistency, not identification, and an in-plane residual below 0.1 px does not tell two photographs 1/3 mm apart from each other), all 220 in the pelvis block above k 2285; each frame-only slice carries its ambiguity set (the photographs whose whole-frame NCC lies within 0.005 of the chosen one: one to three neighbours, 0.33 to 1 mm), block consistency resolves nothing, and the in-plane residual never changes an identity status. The user's instruction after the audit (2026-09-13): no control is lowered because no anatomist takes part; both findings are resolved before the transform feeds RGB-label pairs, training or geometric decisions. Pair selection is explicit and versioned (`scripts/select-cryosection-pairs.py`, policy `pairs-v1`, `generated/cryosection-pair-selection.json`): 2,640 usable (settled), 637 usable-flagged (frame only, flag travels with the pair), 220 excluded with their ambiguity sets, 36 blank Denver slices without reference; per block: feet to knee 1,200 usable, knee to mid-thigh 1,043, mid-thigh to pelvis 397 usable + 631 flagged + 220 excluded. A later rule may resolve excluded slices only by a different criterion, never by a lower threshold. `build-cryosection-transform.py` now refuses partial or unresolved reports (every Denver slice 0 to 3532 exactly once, only `matched` or `denver-blank`, n and offset consistent, no photograph chosen twice), because a `--ks` or truncated `--refine-from` report also carries step 1; `scripts/test-cryosection-transform.py` reproduces the audit's truncated report and six other corruptions. What this does not cover: photographs 1001 to 1553 (head to pelvis) have no aligned Denver counterpart; the transform carries the pelvis block's mapping there as `extrapolated-unverified`, and the photograph-to-CT check against Denver's aligned CT contours is the next stage 0 item. Two publications of the same block compared image to image; nothing here is anatomy.
- Photograph-to-CT check (2026-09-13, plan B stage 0 last item, `scripts/check-photo-ct-alignment.py` on rub-pc, every 5th photograph, 1,033 compared, about 4 min; `generated/photo-ct-alignment-check.json`; debug panels on rub-pc). Every photograph is mapped into the canonical frame with `nlm-cryosection-to-vhf` (measured below the pelvis, extrapolated above), the fresh CT is sampled on the photograph grid through `denver-aligned-ct-voxel-to-vhf`, and the body outlines (photograph tissue against the blue block; CT HU > -400) are compared: Dice, rigid shift by phase correlation of the signed distance maps (CT minus photograph), mean symmetric contour distance before and after removing that shift, and a z scan of the CT (-15 to +15 mm). Baseline on the measured mapping (699 photographs, pelvis to feet): body shift median 5.6 mm, p95 13.3 mm, Dice 0.94; near the pelvis (z 800 to 1,000 mm) shift 3 mm, contour distance 3 to 5 mm; towards the feet the shift grows to 12 mm (CT anterior of the photograph) and the contour distance to 20 to 27 mm even after the shift (the feet and ankles differ in posture and the CT cuts the legs at its field of view); the trend of the y shift with z is -1.07 deg about the left-right axis with 3.3 mm RMS about the line. Above the pelvis (326 photographs, extrapolated mapping): the CT lies posterior and to the subject's right of the photograph by a shift that grows linearly with z, y -12 mm at z 1,300 to -42 mm at z 1,750 and x +10 to +12 mm, equivalent to a rotation of 3.6 deg about the left-right axis and 1.1 deg about the anterior-posterior axis; after removing the per-slice shift the outlines agree to 1.8 to 6.5 mm (thorax and head), the z scan is inconclusive there (best dz on the scan limit for 227 of 326, the in-plane misfit dominates the Dice), and the residual about the linear trend is 9.5 mm RMS. What this identifies: the fresh CT on the table and the frozen block differ by a whole-trunk rotation of about 3.6 deg plus a few-mm residual, once the pelvis is matched; the calculation does not separate the fresh-versus-frozen posture from the CT placement rotation or from a re-orientation of the photograph frame above the pelvis (a rigid re-mounting of the upper blocks would look the same). Consequence recorded in the transform and for stage 1: the photographs above the pelvis have no frozen-state reference; their mapping stays `extrapolated-unverified` with the alignment uncertainty published here (rigid component up to 42 mm at the head against the placed fresh CT, non-rigid residual about 10 mm RMS); fresh-CT trunk labels must not be carried into the cryosection frame with the pelvis fit alone, and any correction needs its own protocol (per-level shift from this report, sensitivity to the two placements) fixed before use. The stage 0 criterion for the upper body (photograph-to-CT residual published as alignment uncertainty, separate from segmentation error) is met as a measurement, not as an alignment. Nothing here is anatomy.
- Codex deep RGB review of 02d3f59 (2026-09-13, 14 slices of the three blocks, ends, transitions and weak identities; evidence archived in `docs/audits/2026-09-13-cryosection-rgb-review/`): the geometry reproduces (RGB luminance against Denver gray NCC 0.978 to 0.998; mirror and 6 px shift controls degrade every case; femur, patella and selected muscle contours follow the visible structures at k 1550 and 3000; identity of near-identical neighbours stays provisional). Priority finding: at the block faces the tissue under Denver's original labels is not observable in the photograph (k 1210 one leg shows blue granular material and dark zones, k 2259 a whitish opaque surface over most of one leg, k 1207 an extensive surface alteration) while identity is settled and NCC is above 0.99; the same appearance is in Denver's gray, so it is not a registration fault, and NCC, file integrity and identity do not protect against it. Applied the same day: `scripts/cryosection-observability-band.py` measures, for the slices of five bands around the block boundaries and ends, the colour of the photograph under the original labels (block-like B > R + 10, dark max < 60, frost-like neutral whitish) and flags slices whose non-tissue fraction exceeds the band interior by 3 MAD and 0.10: k 1205 to 1207 and 1210 to 1211 (frost then block showing through, non-tissue 0.18 to 0.49), 2256 to 2259 (frost 0.26 to 0.57), 2326 to 2327 and 3516, 3520, 3522, 3527 (dark class only, 0.22 to 0.35, no ramp; kept as conservative quarantine and marked so). `registry/cryosection-quarantine.json` (from the curated seed `registry/cryosection-quarantine-seed.json` with the reviewer's evidence plus the band flags; Denver slice and photograph hashes, reason, source) lists 15 quarantined slices; `select-cryosection-pairs.py` applies it before the identity policy as `excluded-observability`, identity never overrides it (6 of the 15 are `settled`), quarantined slices are ignore, never background, and the originals are untouched. Pair selection now: 2,634 usable, 630 usable-flagged, 218 excluded for identity, 15 excluded for observability, 36 blank; per block: feet to knee 1,197 usable, knee to mid-thigh 1,041, mid-thigh to pelvis 396 usable + 628 flagged. `scripts/test-cryosection-transform.py` (14 cases) proves that a settled, quarantined slice is excluded and that a quarantine naming an unknown slice is refused. Only the checked bands were measured: an unflagged slice elsewhere was not inspected for observability.
- RGB pilot, steps 1 to 4 of roadmap 6b prepared (2026-09-13, plan B stage 2 inputs and stage 6 protocol, nothing trained). (1) Aligned RGB volume of Denver block 2 (`scripts/build-cryosection-rgb-block.py` on rub-pc, 28 s with 12 workers; local copy `data/derived/nlm-vhf/cryosections/block2/`, manifest `generated/cryosection-rgb-block2-manifest.json`): 1,248 slices k 2285 to 3532 (z 760.6 to 1,175.8 mm) on the Denver grid, uint8 (k, j, i, 3), 1.08 GB, SHA-256 f7997b57; each photograph is averaged 2 x 2 (as Denver did) and sampled bilinearly through its per-slice similarity; 1,024 slices resampled (396 usable + 628 usable-flagged), 218 excluded-identity and 6 excluded-observability slices left zero and marked ignore; check: NCC of the resampled luminance (0.218 R + 0.710 G + 0.089 B) against Denver's aligned gray slice median 0.9952, p05 0.992, min 0.990, none below 0.95; mirrored resampling as control on 41 slices median 0.62, lower in all. Denver's original labels of the same slices (`scripts/extract-denver-label-block.py`, byte copy of `VHF_Full.mat`, mat SHA-256 fbe07d8d, 62 labels present) written as NIfTI in the canonical frame; the manifest binds every photograph (compressed and raw hashes), Denver slice, transform, pair selection (policy pairs-v1), label slab and output by hash. Finding from the images: the block is not "mid-thigh to iliac crest": above about z 1,010 mm (k 3035) the photographs show kidneys, bowel, liver and lower ribs, and Denver's labels there are the psoas only, so 97 to 99.9 % of the body is unlabelled (ignore) in the upper 165 mm; the supervised part of the block is z 811 to 1,010 mm (ignore fraction of the body 0.66 to 0.79 there, fat, skin, viscera, vessels and nerves being unlabelled). (2) `registry/cryo-tissue-map.json` version 1 (`scripts/build-cryo-tissue-map.py`): all 131 Denver values mapped explicitly (bone 1, cartilage 2, muscle 3, ligament-tendon 4, fat 5 reserved without a Denver source, ignore 255); precedence slice status -> Denver label -> body mask -> background; body mask is the colour rule of the photograph-to-CT check on the Denver grid; Denver 2022 has no composite labels. Applied (`scripts/build-cryo-tissue-classes.py --selftest`, `generated/cryo-tissue-classes-block2.json`, volume SHA-256 a3d44efe): bone 7.9 M, cartilage 0.12 M (femoral heads and acetabula only), muscle 26.1 M, background 99.2 M, ignore 227.5 M voxels; no ligament (knee only) and no fat voxel in the block; 0 labelled pixels outside the colour body mask. (3) `registry/cryo-eval-bands-v1.json` (`scripts/select-cryo-eval-bands.py`, seed 20260913, 1 mm candidate grid, content rule at least 70 % paired slices, bands with 10 mm buffers inside the block and non-overlapping): band 1 k 2528 to 2677 (z 841.5 to 891.1 mm, 141 scoring slices, bone 2.07 M, cartilage 55.7 k, muscle 6.74 M reference voxels), band 2 k 2870 to 3019 (z 955.4 to 1,005.0 mm, 150 scoring slices, bone 2.78 M, muscle 4.12 M, no cartilage); 828 training slices (253 usable, 372 flagged, 203 excluded = ignore) in k 2285 to 2497, 2708 to 2839 and 3050 to 3532; frozen to the manifest, map and label-slab hashes. (4) `registry/machine-acceptance-protocol-v1.json` written before any training: scope block 2, classes bone, cartilage, muscle, use tissue-class segmentation, variants rgb-only and rgb-plus-ct-prior, nnU-Net v2 2D fold 0; criteria bone Dice >= 0.9224 and p95 <= 1.139 mm, muscle Dice >= 0.9095 (H − 0.03) and p95 <= 1.306 mm, cartilage p95 <= 1.382 mm only (Dice reported), minimum 20,000 reference voxels; negative controls mirror (Dice lower by at least 0.05), 2 mm shift, 2 mm dilation, wrong neighbour 10 mm (all must degrade); pooled Dice over the 291 scoring slices, per band and usable-only rows as sensitivity; iteration limit two runs per variant; identity by hash (manifest, volume, map, bands, noise floor now; weights, predictions, versions, seeds at evaluation); statuses `machine-accepted` / `machine-failed` / `machine-not-assessable`; caveat recorded that H_c is per structure and the pilot scores per tissue class, a weaker bar. `scripts/validate-cryo-pilot.py` checks the whole chain (manifest = pair selection per slice, map exhaustive, bands reproduce from the label slab, protocol thresholds equal the noise-floor figures, hashes) and is in the definition of done. Nothing here is anatomy; nothing has been trained; nnU-Net v2 is not installed on rub-pc (import pending the user's decision).
- Codex audit of afec927 applied (2026-09-13 afternoon, one P1 and three P2, all before any training; the first protocol text stays in git history). Codex reproduced the data side in full (hashes, 1,248 label slices byte-identical to the .mat, tissue classes recomputed exactly, 12 RGB slices rebuilt on rub-pc from the original photographs byte-identical, seeded bands reproduced). P1: the protocol borrowed the surface bar `P_c` from `denver-noise-floor.json`, whose figure is marching-cubes vertices against mesh samples in the original-to-final direction only, another metric than the evaluator's. Fix: one frozen metric implementation, `scripts/cryo_metrics.py` (Dice under an eligibility mask; symmetric pooled voxel-surface p95 per k-run with original k coordinates, caps next to ineligible voxels removed from the source set, anisotropic EDT; tolerances 0.01 Dice and 0.333 mm; 20 synthetic cases in `scripts/test-cryo-metrics.py`), and the floor re-measured with it: `scripts/denver-surface-floor.py` -> `generated/denver-surface-floor.json`. **Finding while doing so:** the rasteriser of `denver-noise-floor.py` fills mesh sections with trimesh `polygons_full`, which needs the `rtree` package whenever a slice has more than one closed contour; `rtree` is not installed, the exception was swallowed and every such slice was left empty (27 of 158 slices of the left calcaneus), so 57 of 123 structure Dice values of the published floor were biased low (calcaneus 0.920 -> 0.957, right pelvis 0.847 -> 0.932, cartilage class median 0.59 -> 0.74; bone 0.922 -> 0.932, muscle 0.940 -> 0.941). The new floor uses an even-odd fill of every closed contour (no spatial index) and reports zero section failures; plan B section 2.1 carries the correction; `denver-noise-floor.json` is kept for the record and no longer sets any threshold. Thresholds now (`registry/machine-acceptance-protocol-v1.json`, revised): bone Dice >= 0.9324 and p95 <= 0.9419 mm; muscle Dice >= 0.9105 and p95 <= 0.9419 mm; cartilage p95 <= 0.9704 mm (0.9419 mm is one in-plane diagonal pixel: Denver's two versions differ by about one pixel at the 95th percentile). P2 (surface scoring under-specified at gaps, excluded slices and ignore boundaries): specified in the protocol and the module, tested (gap between bands never creates neighbours, false positives in a run without reference are reported and left out of the surface pool, a missing prediction in a run with reference makes p95 undefined and fails). P2 (controls conflated evaluator and model failure; Codex showed an eroded prediction improving under dilation with a correct evaluator): evaluator sanity controls (reference perturbed against reference: mirror, +3 px, 3 px dilation; failure = every class not assessable) are now separate from model-side controls (prediction perturbed, wrong neighbour 10 mm on matched slices and eligibility; failure = that class machine-failed, control named). P2 (validator checked hash pointers, not rows): `scripts/validate-cryo-pilot.py` now compares every paired manifest row (n, nlm, tc, tr, s, theta, identity status) with the transform, photograph hashes with the inventory, map names with the source names, training eligibility and supervision bins with the tissue-classes report; `scripts/test-cryo-pilot-validator.py` runs 27 in-memory corruptions that must each fail. Codex feedback A to F adopted: training eligibility split by supervision density in `registry/cryo-eval-bands-v1.json` (a 50 mm bin whose paired slices have a median ignore fraction of the body above 0.95 is sparsely supervised: Denver labels only the psoas there): primary 197 paired slices (k 2285 to 2497 and 2708 to 2839), auxiliary 428 (k 3050 to 3532, off by default, a separate decision), 203 excluded slices never sampled, bands unchanged; the 11 paired slices of k 2285 to 2435 listed; false positives of a class in a band without reference count in the pooled Dice and are reported, not in the surface pool; tolerance and tie policy fixed; dilation kernel (2D cross, 3 iterations, per class binary) fixed; the mirror is a frame reflection and the protocol says so; classes 4 and 5 are reserved ids, not network outputs, never learned as negatives. `scripts/cryo-pilot-evaluate.py` implements the protocol (a prediction volume in, the acceptance report out; `--oracle` scores the reference against itself, 12.5 min, and freezes the evaluator sanity figures in `generated/cryo-pilot-oracle-controls-block2.json`: evaluator trusted; reference-control Dice bone mirror 0.789, shift 0.944, dilation 0.970, 10 mm neighbour 0.780; muscle 0.838, 0.959, 0.988, 0.854; cartilage 0.064, 0.474, 0.697, 0.395, equal to three decimals to the figures Codex measured independently with its own code). Both new tests join the definition of done. Nothing trained; nothing here is anatomy.
- Codex audit of 87ff582 applied (2026-09-13 afternoon, two P1 and three P2, before any training; Codex reproduced the rasteriser against independent ray containment on three structures with zero mismatches and the three structure floors to every digit). P1 (the evaluator graded any input): `scripts/cryo-pilot-evaluate.py` now passes four gates before any score: the registry validator, the identity chain (actual manifest, tissue-classes volume and report, bands, map, both floors, metrics module and evaluator against `identity_by_hash.fixed_now`, byte for byte), the prediction grid and values (same shape and affine, integral dtype, finite, only the active output classes; nothing cast), and, for a non-oracle run, a training manifest (slices inside the primary eligibility and outside bands, buffers and the auxiliary stratum; weights hash, nnU-Net version, dataset fingerprint, plans, seed, fold, run history within the iteration limit). A failed gate writes a report with every class `machine-not-assessable` and exits 1; `scripts/test-cryo-pilot-evaluator.py` (11 cases: translated affine, int16 256.., float, foreign label, foreign block, missing or contaminated training manifest, iteration limit) proves it. P2 (cap voxels acted as distance targets; a reference filling the run gave a finite p95): caps are removed from both surface sets, support kept and removed is reported, a mask without observable surface is undefined and fails. P2 (a missing prediction in a single-slice run went undetected): emptiness is decided per run before surface extraction; the guarantee is per run, not per component, and the protocol and the cartilage rule say so. P2 (run diagnostics stripped): the report keeps every `per_run` record and adds per-band prediction, reference, false-positive and false-negative voxel counts and reference/prediction-bearing slice counts. `scripts/test-cryo-metrics.py` grows to 25 cases (Codex's cap-target, nested-box and single-slice-run counterexamples, the omitted-component limit). Rasteriser: refuses open contours and dangling entities instead of leaving an empty slice; `scripts/test-denver-rasteriser.py` (6 analytic cases: one box, disjoint, nested hole, overlap = XOR by convention, open contour refused); watertightness and body count recorded per structure (three meshes not watertight: left phalanges, right talus, right peroneus longus; left talus cartilage has two bodies; every section closed). Floor re-measured with the revised metric: class medians unchanged (bone 0.9324 / 0.942 mm, muscle 0.9405 / 0.942, cartilage 0.7449 / 0.970). Protocol v1 revised a second time (gates, symmetric caps, per-run guarantee, report contents, hashes); validator and 27 corruption cases pass; the oracle run repeated with the revised evaluator. Both new tests join the definition of done. Nothing trained. nnU-Net v2 2.8.1 (Apache-2.0) installed on rub-pc in `/media/rub/Backups/VHF/env-nnunet` (torch 2.14.0+cu130, CUDA on the RTX 3080) on the user's decision of 2026-09-13; no dataset prepared yet.
- UI: sources HRA, TCIA, Denver, NLM VHF CT, CT consensus instances, composite, BodyParts3D; model-agreement panel for consensus instances with gates and Denver comparison for bone candidates; donor filter; registration review panel with landmark overlay and exportable decisions; side-by-side source comparison for structures with alternatives; coverage filters for multi-source, partial (grouped label) and crosswalk-resolved entries; ontology term and mapping in every provenance panel.

## Plan audit

| Phase | Evidence | Remaining work |
| --- | --- | --- |
| 0 Survey | `datasets.csv`, 15 sources, licences verified with evidence URLs and dates for 13 | BMFToolkit data scope (authors); Open3Dmodel and AnatomyTool per-asset terms. |
| 1 Ontology | Reviewed crosswalk with OLS evidence for Denver, TCIA, NLM CT, HRA and BodyParts3D lateralized terms | Anatomist review of equivalences; 168 unresolved entries; TA2 import; full UBERON universe beyond the imported union. |
| 2 Coverage | Coverage matrix with individual, grouped and partial kinds, per-source candidates, 248 multi-source entries | Register candidate availability outside imported data (SPIDER, OpenEar, HiP-CT specimens). |
| 3 VHF base | Canonical space verified against NLM CT headers (rotation 2.2 deg, scale 1.001); trunk, upper limb and head of the same donor from the NLM CT labels; Denver lower limb | Hands, forearm bones, patella, tibia, fibula and feet of the VHF donor from CT need the licensed TotalSegmentator `appendicular_bones` task (not usable) or another segmenter; cryosection-based trunk segmentation; anatomist landmark review. |
| 4 CT scaffold | TCIA 003 (alternative source), NLM VHF CT (composite trunk); three-model bone comparison and laterality consistency checks on the NLM and Denver CTs; per-instance consensus of vertebrae and ribs ingested as source `ct-consensus` and composed (0.5) | Instance consensus for the other bones (they agree by name); lumbosacral `S1` question; names pending; CT femur pose differs from the cryosections by 1.6 to 3.0 deg (Denver bones used). |
| 5 Specialized | Licences and specimen metadata verified for SPIDER, OpenEar, HiP-CT, Teeth3DS, SPARC | Specimen selection with documented sex and frame; import as overlays. |
| 6 Fallback | BodyParts3D comparison reference merged through the crosswalk; Z-Anatomy audited (CC BY-SA, male-derived) | None planned for the open-clean target. |
| 7 QA | Geometry, self-intersections, components, outliers, continuity, licence checks, registration evidence, review registries; QA identity by SHA-256 with verified carry-over, regression test, source and composed QA digests validated in the composite | Anatomist review of every mesh, landmark and fit; `registry/review-status.json` is empty except known issues; self-intersections of the voxel-derived CT and consensus meshes are counted, not repaired. |
| 8 Web atlas | Six sources, donor filter, registration review, comparison, coverage filters, provenance with ontology, model-agreement panel for consensus instances | Landmark editing in 3D (decisions only today); multiresolution overlays for future specialist sources. |

## Known limitations

- Every registration is automatic. The same-donor CT fit is rigid and tight at the pelvis (p95 4.7 mm) but the CT sacrum differs from the Denver sacrum by 9.8 mm p95 (extent and coccyx), and the femora moved 1.6 to 3.0 deg at the hip between acquisitions; the CT trunk above the pelvis differs from the frozen block by a whole-trunk rotation of about 3.6 deg (42 mm at the head against the photographs, `generated/photo-ct-alignment-check.json`), measured on 2026-09-13 and not corrected; the only CT of this donor is the fresh one (Denver's aligned CT is a resampled copy of it).
- The two CT exams (vertex to thigh, thigh to feet) were stacked with a 1 mm continuity assumption; the measured in-plane shift at the junction (5.6 x 3.8 mm) is recorded, not corrected. Only exam 370 (trunk) enters the composite; the exam 371 legs are covered by Denver.
- TotalSegmentator labels are model output on a 1993 cadaver CT; boundaries, missing labels (prostate, kidney cysts, portal vein union) and label extents are unreviewed. Stray islands below 5 % of a label were removed and counted.
- HRA organs are placed by a six-proxy similarity (RMS 7.4 mm on bounding-box centres), which does not validate organ shape or position; HRA organs with the same term as a CT label are dropped, so the composite mixes one measured donor with a reference assembly.
- The composite has no VHF hands, forearm bones, patellae, tibiae, fibulae or feet from CT: those come from Denver (feet, patellae, tibiae, fibulae) or are missing (hands, radius, ulna). TCIA forearm bones are not composed.
- Landmarks are automatic geometric rules; slab centroids sit up to 6.6 mm off the surface. Manual landmark review is pending.
- CT tissue depots (TCIA) and CT muscle compartments (NLM autochthon) contain nonmanifold edges or self-intersections; they are preserved as labelled compartments, not repaired. Every consensus vertebra and rib mesh also carries a few self-intersecting pairs (median 6) from the same voxel-to-mesh pipeline.
- Composition 0.5 shipped between 2026-09-10 and 2026-09-12 with `not-assessed` QA fields in manifests and viewer, and without QA metadata on the consensus instances in the composite (commit `b0063fd`); corrected in `083589a` and `553ecfb`, with the validator now refusing that state.
- Plan B section 3.6 (agentic visual review of cryosections) is an optional experiment whose adoption and abandonment criteria are incremental (errors removed against current controls, false alarms, cost, pre-registered thresholds); the verifier is a second segmenter with its own proposal, not an independent validator, and regions without a Denver reference limit the conclusions instead of cancelling the pilot.
- `Toes` (TCIA) has outlying geometry above the head; it is flagged in `generated/anatomy-qa.json` and excluded from the composite.
- Source-local labels resolve to UBERON/FMA by exact label or synonym with OLS evidence, but no anatomist has confirmed the equivalences; 168 entries remain unresolved.
- Upstream npm installation reports 11 vulnerabilities. Dependency remediation has not yet been evaluated.

## Next work

Active viewer task (subsequent user implementation request, 2026-09-13): [synchronized 3D and slice viewer specification](plans/synchronized-slices-viewer-plan.md), phases 0–4. RGB remains a separate extension. Phase 0 decisions are frozen in [synchronized-slices-decisions.md](plans/synchronized-slices-decisions.md): actual input hashes/grid and organ ids resolved, abdominal crop and 171 × 171 × 104 preview selected, display orientations, support semantics, tolerances and resource budgets documented. `app/slices/{types,coordinates,sampler}.ts` implements stage/voxel transforms, shared orthogonal/oblique plane descriptors, pixel projection, mixed direction labels and the CPU reference sampler (linear HU, nearest labels, conservative support). `scripts/test-slices.mjs` passes an asymmetric analytical phantom including a deliberate mirror failure, anisotropy/crop/rotation, integer centres, tilted cuts, pivot/translation, picks and gaps. This is foundation code; there is no Slices UI or served volume yet. Next: phase 1 builder, manifest/loader integrity gates and native-slice reproduction; then phases 2–4 UI, shared textured planes, GPU prototype and actual browser/performance acceptance. Existing cryosection training is outside this task and untouched.

Phase 0 checks on base `a4754fa4071ff18e6d48ffb8c5fbad334e89ede7` plus uncommitted viewer changes: all 14 commands in the `CLAUDE.md` definition-of-done block passed, plus `node --experimental-strip-types scripts/test-slices.mjs`, `npm run build` and `git diff --check` (2026-09-13; terminal log `/tmp/slices-phase0-checks.log`). The production build reports its existing >500 kB chunk warning. No browser checks, actual volume reconstruction, GPU comparison, resource benchmarks or anatomical review have occurred for Slices; first-release completion remains unproven.

Phase 1 implementation (2026-09-13, same base plus uncommitted changes): `scripts/build-viewer-volume.py --serve` now builds the real abdominal package in ignored `data/derived/viewer/nlm-abdomen/` and copies it to ignored `public/volumes/nlm-abdomen/`. 18,246,384 decoded bytes; measured 3.327 s initially, 2.796 s on repeat (offline Python peak RSS 1,437,344 KiB). `scripts/test-viewer-volume.py` passes full retained native CT/label equality (3,041,064 centres each), crop/stage checks, reproducibility, invalid-label rejection and a gap between preview centres. `app/slices/volume-loader.ts` checks metadata, resource limits, chunk size/hash, current source atlas hash, source identities, affines, exact provenance and label/geometry joins, and cancels/rejects stale loads. `scripts/test-volume-loader.mjs` passes real loading plus 24 corruptions, absent/truncated/tampered buffers, changed atlas, cancellation and supersession. All 14 working-agreement commands, the coordinate test, production build and diff check passed again (`/tmp/slices-phase1-checks.log`; focused new tests also passed). See `docs/REPRODUCIBILITY.md` for preparation commands. Phase 1 data/loader foundation is implemented; there is still no Slices UI, browser inspection, GPU prototype or browser performance evidence. Next required work is the orthogonal/oblique workspace and shared textured 3D planes, followed by all release acceptance checks. The objective stays active; no source data, training, composition, commits or deployment changed.

Order and acceptance criteria are in `docs/plans/female-open-human-atlas-plan.md`, section 29 (synchronised 2026-09-13). Decision of 2026-09-13 (user; agreed by Claude and the Codex auditor): no anatomist takes part in this project; the blinded audit becomes an optional stage 8; the terminal status is `machine-accepted` (plan B section 2.7, `CLAUDE.md`); block C (cryosection segmentation) is the current work and the CT atlas is touched only as a prerequisite. Backlog from the Codex audit of aa71a89 (P2, not blocking): `validate-consensus-metadata.py` must check that `shape_check.baseline_bone` is the candidate's own class and side and cross-check `generated/ct-candidate-shape-check-nlm.json`; `ct-candidate-shape-check.py` reads `public/atlases/ct-consensus.json`, which `build-registry.py` refreshes, so the documented order must run it after `build-registry` or read the optimized model directly. Backlog from the Codex audit of 61ccf5c (P2, not blocking, 2026-09-13): `validate-consensus-metadata.py` compares the ids of `trunk_posture_context.linked_levels` but not their nested figures (offset vectors, rotation, threshold, candidate name) against `generated/trunk-posture-offset.json`; add that comparison and a test that corrupts both the source atlas and the manifest copy so the check is isolated. The published values match today. Backlog (2026-09-13, from the CT provenance correction): the `posture_offset` / `trunk_posture_context` fields, their glosses in `ingest-ct-consensus.py`, `ingest-nlm-vhf.py`, `validate-consensus-metadata.py`, `app/agreement.ts` and the tests call a placement inconsistency between two rigid fits of one CT a posture offset; renaming them needs the full pipeline and a decision. Next audit exchange is at the RGB-pilot milestone.

1. Block C (current work): VHF appendicular bones, hands, trunk and head from the colour cryosections, see `docs/plans/vhf-cryosection-machine-driven-plan.md` (Denver also publishes the full-body CT aligned to the cryosection frame, a cross-check for `nlm-ct-to-vhf`). Anatomist review only for what remains after the automatic checks: landmarks (viewer export -> `registry/landmark-review.json`), CT label boundaries, HRA placement; then refit from confirmed landmarks only.
2. Stage 0 of block C: trunk posture offset measured on 2026-09-13 (bullet above; pelvis-anchor offset 3.35 mm of unidentified cause, about 2 mm relative to the pelvis in the thoracic and lumbar spine, about 5 mm at C1 to C3 and the skull, nothing corrected, nothing attributed). Alignment of the colour photographs against Denver's aligned slices done on 2026-09-13 (bullet above; three rigid blocks, residual below 0.1 px on every slice, `transforms/nlm-cryosection-to-vhf.json`, extrapolation above the pelvis marked unverified). Photograph-to-CT check done on 2026-09-13 (bullet above): above the pelvis the placed fresh CT and the photographs differ by a whole-trunk rotation of about 3.6 deg (42 mm at the head), the photographs there keep `extrapolated-unverified` with that uncertainty published, and Denver's aligned CT turned out to be the same fresh CT (correction bullet above). Stage 0 is complete as measurement. RGB pilot steps 1 to 4 done on 2026-09-13 (bullet above: aligned RGB volume of block 2 with manifest, tissue map v1 applied, two frozen evaluation bands, pre-registered acceptance protocol v1, validator). Step 5 prepared and training started on 2026-09-13 (user decision: both variants, default `nnUNetTrainer`, 1000 epochs). `registry/cryo-ct-prior-map.json` maps all 117 TotalSegmentator `total` labels to a pilot class (62 bone, 10 muscle, 1 cartilage, 44 none) and is a SEPARATE registry so the frozen identity hashes of `registry/machine-acceptance-protocol-v1.json` stay valid; `scripts/build-cryo-ct-prior-block.py` carries the fresh-CT labels onto the block grid by nearest neighbour through `nlm-ct-to-vhf` (`generated/cryo-ct-prior-block2.json`). Measured prior coverage on the 197 primary slices: bone recall of the Denver reference 0.9196, muscle 0.5232, cartilage 0.0 (`costal_cartilages` is the only cartilage label of the task and lies above these slices). The plan's "3 to 5 mm pelvis placement error" does not describe this block: it is mostly thigh, where the pelvis-fitted rigid transform places the femora 7.5 to 8.7 mm off (their own rigid fits, 3.4 to 4.4 mm, are measured and applied nowhere). `scripts/build-cryo-nnunet-dataset.py` writes the two 2D datasets from the primary eligibility only (197 paired slices, bands, buffers and the auxiliary stratum refused in code) plus the 291 paired band slices as `imagesTs`, and a frozen split blocked by the 50 mm bins (4 folds; fold 0 is trained because its bin holds the fewest primary slices and so leaves 187 of 197 for training, a rule fixed before any score existed; inference uses `checkpoint_final`, so the validation fold reports and never selects). Two encodings are local to the derived dataset and never touch the reference: ignore is 4 there and 255 in `tissue-classes.nii.gz` (nnU-Net asserts ignore == max(labels) + 1 and refuses 255; the builder refuses a source volume that already holds 4 or 5), and the singleton axis carries a dummy 999 mm spacing (with the true 0.333 mm nnU-Net reordered the axes by spacing and planned a `[1, 704]` patch, one-pixel strips instead of slices; the preprocessing gate now fails on a degenerate plan). Plan actually produced: patch 448 x 704, batch 9, spacing 0.666 mm, one 434 x 666 slice per case, RGB->0-1 on the colour channels and no normalisation on the prior channels. `scripts/record-nnunet-run.py` reads the run back from the training box (nnU-Net 2.8.1 from the distribution metadata, torch and GPU, one entry per `training_log_*.txt` as the observed run history, checkpoint hashes, and the seed read out of the launcher) and `scripts/write-cryo-train-manifest.py` refuses a declared run history that disagrees with the logs, so the protocol's two-run limit is checked against disk instead of being free text. Seeding limit: `nnUNetv2_train` has no seed flag; the launcher seeds the main process with 12345, nnU-Net passes `seeds=None` to its batch-generator workers and cuDNN benchmarking is on, so the run is NOT bit-reproducible. Measured cost on the RTX 3080 at a 250 W power cap (82 C, down from 87 C at the 320 W default): about 44.5 s per steady-state epoch, so about 12.4 h per variant; the first epoch takes 110.7 s and is not representative. `rgb-only` started 2026-09-13 17:50; `rgb-plus-ct-prior` follows. Training is resumable from `checkpoint_latest.pth` (saved every 50 epochs) with `run-cryo-resume.sh`. An adversarial audit of this code by a separate agent found no test leakage, no grid or mirror mismatch between image and label (checked numerically on the real block), and no path by which the prior reaches the target, the reference, the metrics or the identity hashes; its four findings (operator-typed seed and version, unverified run history, a test that skipped silently, no regression test for the real transpose path) are fixed above and in `scripts/test-cryo-nnunet-dataset.py`, now 19 checks. The whole post-training chain was rehearsed on 2026-09-13 with a synthetic prediction (the reference with ignore mapped to background) while the GPU was busy, because none of it had ever been executed: assembly of 291 band slices onto the reference grid, both refusals of the manifest writer (an unfinished run, and a declared history that hides an attempt), the happy path, and the evaluator's non-oracle route, which took 447 s and reported `evaluator_trusted: true` with the model-side controls degrading in the expected order (mirror < shift < dilation) and the wrong-neighbour baseline below the prediction for every class. The Dice of 1.0 there is not a result: the prediction WAS the reference. One real defect was found and fixed by the rehearsal: `write-cryo-train-manifest.py` raised `ValueError` on an `--out` outside the repository. Codex audit of 909e500 applied on 2026-09-13, while the GPU kept training (evidence kept: the auditor's own
`/tmp/audit-rgb-909e500.py`). Three P1, all reproduced before and after the fix. (1) `assemble-cryo-prediction.py` read
the class values through `int()` and cast with `astype(uint8)`: both truncate, so a float slice holding 1.9 became class
1 and the assembled volume then passed the evaluator's prediction gate; it also ignored the input affine, so a slice
placed a metre away was assembled in silence. A slice gate now refuses a fractional or non-finite value and an affine
that is not this dataset's 2D case grid, BEFORE anything is converted. (2) `write-cryo-train-manifest.py` combined the
report chosen by `--variant` with any provenance file, so an rgb-only report and a provenance naming Dataset502 with six
channels was written and accepted; `bind_gate` now checks that the run's paths name the report's dataset, that the
trained channel count matches, and that the installed split is the frozen one, which needs the new `split_counts` the
recorder reads from `splits_final.json`. (3) the writer SAVED a rejected manifest before exiting 1, and the evaluator's
training gate reads neither `consistency_problems` nor `runs_observed`, so the rejected file passed it; the evaluator is
frozen by hash and is untouched, and a rejected manifest now goes to `<out>.rejected.json` so no acceptable file exists
at all. P2 applied: `run-cryo-preprocess.sh` exits non-zero when its sentinel says `ok: false`; the dataset test now
runs the builder itself into a temporary tree and compares every colour and prior channel and every target against the
sources (a deliberate transpose regression makes it fail, checked), instead of reading the shapes of files already on
disk; the recorder's `EPOCH_LINE` never matched nnU-Net's real `2026-09-13 17:50:20.236377: Epoch 0` because the
timestamp pattern could not cross the colons of the clock, so `last_epoch_logged` was always null, and the seed pattern
saw only `random.seed`, so a launcher seeding torch differently would have been reported as one clean seed. Three
claims of ours were wrong and are corrected: neighbouring slices DO straddle train and validation across a bin boundary
(minimum distance one slice, 0.333 mm, in all four folds; what the blocking prevents is a shuffled-slice split); "Meta
claims no rights over a mask" is our reading of the Agreement's silence, not an express grant; and reverse engineering
is SAM License 1.b.iv, not 1.b.v. The 7.5 to 8.7 mm figure is restated as what it is, a surface distance between the
placed CT femur and the Denver femur, not a uniform per-voxel offset of the prior; the frozen protocol keeps its "3 to
5 mm" wording and the erratum is recorded in `registry/cryo-ct-prior-map.json` instead, because editing the protocol
would void version 1. Found while applying the audit: `build-cryo-ct-prior-block.py` and `build-cryo-nnunet-dataset.py`
wrote their report to a fixed path whatever `--out` said, so a throwaway audit run replaced the committed report with
one naming a volume that never existed there, and `git add -A` carried it into `c060d45`; the volume hash was and is
unchanged, only the recorded name was false. A report now follows the output it describes. Still open, not fixed: the
observed run count is a MINIMUM, since a deleted training log is invisible and the record is read at the end of a run,
so a tamper-proof count needs a per-start record kept outside the replaceable results folder; and
`run-cryo-train.sh`, `run-cryo-resume.sh` and `run-cryo-chain.sh` keep their error-handling defects until the GPU is
free, because editing them now would either corrupt the running bash or desynchronise the launcher hash the provenance
records. Still to come: the real predictions, then `scripts/cryo-pilot-evaluate.py` once per variant; then step 6 (SAM 3 second opinion). Its licence prerequisite is done (2026-09-13, plan section 3.3): the SAM License was read in full from `facebookresearch/sam3/LICENSE`. No clause restricts the outputs, so SAM 3 masks may ship under the atlas licence; 1.b.i puts the trained weights inside "SAM Materials", so the atlas ships no SAM weights and no fine-tuned checkpoint; 1.b.ii obliges acknowledgement in any publication; 1.b.v prohibits reverse engineering and the ITAR, military, nuclear, espionage and weapons uses. No user or revenue threshold. Commercial use is neither granted in words nor prohibited, which is weaker than CC BY 4.0 and is recorded as such. Medical-SAM3 ships the SAM License itself, so it is a derivative on the same terms; the plan's "licence file not read yet" is closed and the plan B section 9 decision. Before any fresh-CT label is used above the pelvis, a per-level correction protocol from the photograph-to-CT shifts, fixed before use. The original item: measure the fresh-CT versus frozen-block trunk posture (vertebral landmarks against the cryosections and Denver's aligned CT) before any CT label is used as a trunk guide in the cryosection project: segmenter agreement does not correct registration or posture.
3. Lumbosacral `S1` dossier (backlog, checks run from the cryosections when block C reaches the pelvis) (`docs/plans/vhf-s1-lumbosacral-dossier.md`): investigate with the cryosections, same-donor references and the nomenclature alternatives; resolve only if the evidence allows, otherwise keep the geometric ids and the pending name. It does not block any other item.
4. CT bone candidates (backlog; only when block C needs them): per-name bone candidates: batch run, the 9 `candidate-consensus` bones ingested as `machine-unverified` alternatives and the shape check of section 2.6 recorded per bone (femora reach the Denver baseline, hips above it, five classes without baseline; see above). Next: a substitution decision per bone against `nlm-vhf-ct` recorded in `registry/composition-recipe.json` (the femora are the only candidates that meet every criterion; the decision must weigh that the check is against the candidate's own CT); a shape baseline for classes Denver does not cover needs another measured female reference or stays open. Denver stays wherever it is used today unless a documented comparison and a recorded decision say otherwise. Humeri and forearms are cut by the CT field of view and wait for the cryosections; the MOOSE-only single-model candidates stay in `data/derived` until a second model or the cryosections can vote.
5. BMFToolkit: send the drafted licence request; import as a comparison source if the authors confirm.
6. Specialist overlays: select one female SPIDER lumbar study and one OpenEar specimen with documented sex; import as registered overlays in their own frames.
7. Ontology: review the 168 unresolved crosswalk entries; import TA2 identifiers.
8. Dependency remediation for the upstream npm vulnerabilities.

All large input data are outside Git under `data/raw/`, `data/derived/` or `sources/`. Nothing has
been published, deployed externally, or represented as anatomically validated.
